# 專案規則

「任務計時器」單頁專案（`index.html`）。以下規則每次修改這個專案都必須遵守：

1. **深色星空背景，主色只用在當下要強調的那一個元素上。** 主色是 `--primary`（橘色 `#ff6a3d`）。不要把主色用在裝飾性或次要的地方，只留給當下最需要被注意的那個元素（例如「發射」是唯一實心主色按鈕、進度條/火箭代表正在進行中的動作）。

2. **星球與火箭一律用 canvas 或 CSS 畫，不得引用外部圖片。** 行內 SVG（直接寫在 HTML 裡的 `<svg>`，不是外部圖檔）視為符合這條規則。目前星球是 canvas 逐點畫的粒子點陣，火箭是行內 SVG——兩者都合規，之後修改也要維持這個做法。

3. **按鈕文案使用航太語彙：發射、待機、返航、補給。** 這是固定的詞彙庫，之後新增或修改按鈕都要從這幾個詞裡面選，不要用「開始」「暫停」「重置」之類的一般詞彙。目前只用到發射／待機／返航三個，「補給」保留給未來功能使用。

4. **倒數的分鐘數要放在程式碼最上面當一個設定值，不要散落在程式碼各處。** 目前是 `var TOTAL = 25 * 60;`，宣告在 script 最開頭。之後如果要讓時長可調整，也要維持「唯一一個常數，放在最上面」的做法，不要把秒數/分鐘數的計算或寫死的數字散落到其他函式裡。
